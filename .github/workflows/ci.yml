name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: .
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install -r requirements-tcga.txt

      - name: Run tests
        working-directory: .
        run: pytest -q

      - name: Script compile smoke check
        working-directory: .
        run: |
          python -m py_compile run_simulation.py
          python -m py_compile run_phase2_extractions.py
          python -m py_compile examples/run_full_scenario.py
          python -m py_compile experiments/tcga/ci_validate_committed_outputs.py
          python -m py_compile experiments/tcga/tcga_extract.py
          python -m py_compile experiments/tcga/tcga_extract_expanded.py

      - name: TCGA reproducibility validation (committed artifacts)
        working-directory: .
        run: |
          python experiments/tcga/ci_validate_committed_outputs.py
